import { Text } from '../Text';

export const LoadingPersist = () => {
  return (
    <div className="h-screen w-full bg-black flex justify-center items-center">
      <Text fontSize={30}>Loading...</Text>
    </div>
  );
};
